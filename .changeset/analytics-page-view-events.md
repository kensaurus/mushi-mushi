---
'@mushi-mushi/core': patch
---

Two event names join the analytics taxonomy (`MUSHI_EVENTS`): `page_view`, the name `analytics.autoPageviews` page views use on web, docs and console surfaces, and `docs_page_view`, which the docs site sends on every route with a `route` property.
