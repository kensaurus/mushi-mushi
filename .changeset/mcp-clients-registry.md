---
"@mushi-mushi/mcp": minor
---

Add the shared `@mushi-mushi/mcp/clients` registry — a single source of truth for the supported AI clients (Cursor, VS Code, Windsurf, Cline, Claude Code, Claude Desktop, Zed, …) and how to install the Mushi MCP server into each. Consumed by both the admin console one-click connect flow and the public docs `/connect` landing so the two never drift.
