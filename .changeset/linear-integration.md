---
"@mushi-mushi/node": minor
"@mushi-mushi/plugin-linear": minor
---

Add Linear integration — `LinearConnector` in `@mushi-mushi/node` for programmatic OAuth management and connection status, plus updated `@mushi-mushi/plugin-linear` routing to use the new platform integration API.
