---
"@mushi-mushi/mcp": patch
---

Ship a `server.json` MCP registry manifest (modelcontextprotocol.io 2025-09-29 schema) so the server can be listed in the official MCP registry, and minor server/catalog metadata touch-ups.
