{
  "name": "@mushi-mushi/inventory-auth-runner",
  "version": "0.1.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "description": "Runs the inventory.yaml auth.scripted block via Playwright and seeds the resulting cookies into project_settings so the Mushi crawler + synthetic monitor can hit auth-gated routes.",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "mushi-mushi-auth": "src/run.ts"
  },
  "scripts": {
    "auth": "tsx src/run.ts",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "build": "echo 'inventory-auth-runner: no build step — runs via tsx'"
  },
  "dependencies": {
    "@playwright/test": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.2"
  }
}
