package dev.mushimushi.sdk

/** Compile-time SDK metadata. Update when bumping the package version. */
object MushiInfo {
    const val SDK_NAME = "@mushi-mushi/android"
    const val SDK_VERSION = "0.2.0"
}
