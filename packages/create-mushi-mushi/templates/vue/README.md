# Mushi Mushi — Vue 3 starter

```bash
npm install
cp .env.example .env.local   # then fill in the two vars, or run: npx mushi-mushi
npm run dev
```

`npx mushi-mushi` signs you in via the browser and writes `.env.local` for you.
Docs: https://kensaur.us/mushi-mushi/docs/quickstart/vue
