import Foundation

/// Compile-time SDK metadata. Update when bumping the package version.
public enum MushiInfo {
    public static let sdkName = "@mushi-mushi/ios"
    public static let sdkVersion = "0.2.1"
}
