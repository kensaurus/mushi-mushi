# Changelog

## 0.17.0

- Initial release.
- Registers the Mushi MCP server with VS Code agent mode via
  `vscode.lm.registerMcpServerDefinitionProvider` (stdio + hosted HTTP transports).
- API key prompt stored in VS Code SecretStorage.
- `Mushi Mushi: Use the read-only demo (no signup)` command for keyless trial.
