# Mushi Mushi React Demo

Minimal demo app showing the Mushi Mushi widget integrated into a React app.

## Setup

```bash
cp .env.example .env   # fill in your Mushi Mushi credentials
pnpm install
pnpm dev
```

The demo includes test buttons for: dead clicks, thrown errors, failed API calls, and console errors — all captured by the SDK.
