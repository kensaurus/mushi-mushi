/* PostCSS config for the docs site. Tailwind v4 uses a dedicated PostCSS
 * plugin (`@tailwindcss/postcss`) instead of the v3 `tailwindcss` plugin. */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
