/* PostCSS config for apps/testers — Next.js + Tailwind v4.
 * Tailwind v4 uses @tailwindcss/postcss instead of the v3 tailwindcss plugin. */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
